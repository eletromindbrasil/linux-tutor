# Copie para .env. O script iniciar.sh gera os segredos ausentes automaticamente.
DB_PASSWORD=troque_por_um_segredo_aleatorio
INSTANCE_ID=linux-tutor-producao

# Credencial temporária criada somente se o usuário ainda não existir.
INITIAL_ADMIN_EMAIL=eletromind.brasil@gmail.com
INITIAL_ADMIN_PASSWORD=12345678

# URL pública exata quando houver proxy reverso (sem barra final).
# APP_ORIGIN=https://linux.exemplo.com
# COOKIE_SECURE=true
